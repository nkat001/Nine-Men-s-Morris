package game.Action;

import game.Position;
import game.Token;

/**
 * Jump that represent an action
 * <p>
 * Created by: Ethel
 *
 * @author Nethara
 */
public class Jump implements Action {

    /**
     * To execute the action on the token
     *
     * @param token
     * @return String
     */
    public Boolean execute(Token token, Position initP, Position newP) {
        Boolean b = false;
        return b;
    }
}
